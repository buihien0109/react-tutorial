
import TodoList from './features/TodoList';

function App() {
  // const dispatch = useDispatch();

  // useEffect(() => {
  //   dispatch(fetchTodos())
  // })

  return (
    <div className="App">
      <TodoList />
    </div>
  );
}

export default App;
